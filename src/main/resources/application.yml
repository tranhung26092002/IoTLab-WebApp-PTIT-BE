server:
  port: 8088
  address: 0.0.0.0

spring:
  application:
    name: API-GATEWAY
    cloud:
        gateway:
          globalcors:
            add-to-simple-url-handler-mapping: true

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://14.225.206.27:8761/eureka}
