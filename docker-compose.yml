version: '3.8'

services:
  api-gateway:
    build:
      context: .
    container_name: api-gateway
    ports:
      - "8088:8088"
    networks:
      - ptit-net
    restart: always

networks:
  ptit-net:
    driver: bridge
