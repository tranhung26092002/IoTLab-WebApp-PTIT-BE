version: '3.8'

services:
  api-gateway:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: api-gateway
    image: api-gateway 
    ports:
      - "8088:8088"
    environment:
      - EUREKA_SERVER=http://eureka-service:8761/eureka  
    depends_on:
      - eureka-service
    deploy:
      resources:
        limits:
          memory: 500M
    networks:
      - ptit-net
    restart: always

networks:
  ptit-net:
    external:
      name: ptit-net
